name: CI
on:
  push:
  pull_request:
  workflow_dispatch:
jobs:
  build:
    name: test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run checks
        run: echo "✅ CI läuft"
